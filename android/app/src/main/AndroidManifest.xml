<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Critical: Microphone access for voice expense logging -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Internet for Gemini AI API and Google Speech -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Required for reminders on Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Required for speech recognition activity on some devices -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <!-- Declare microphone hardware feature as optional (don't block install) -->
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <queries>
        <!-- Required to find speech recognition services -->
        <intent>
            <action android:name="android.speech.RecognitionService"/>
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH"/>
        </intent>
        <!-- Required by Flutter for process text -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

    <application
        android:label="Amar Wallet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
